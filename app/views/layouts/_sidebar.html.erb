<nav class="nav nav-sidebar">
  <ul class="nav bs-docs-sidenav">
    <li class="<%= 'active' if params[:controller] == 'purchases' %>">
    	<a href="<%= purchase_cart_path unless session[:purchase_cart].nil? || session[:purchase_cart].empty? %>">
    		<span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span> Purchase Cart <span class="badge"><%= session[:purchase_cart].count unless session[:purchase_cart].nil? || session[:purchase_cart].empty? %></span>
    	</a>
    </li>
    <li class="<%= 'active' if params[:controller] == 'sales' %>">
        <a href="<%= msale_cart_path unless session[:msale_cart].nil? || session[:msale_cart].empty? %>">
            <span class="glyphicon glyphicon-usd" aria-hidden="true"></span> Machine Sale Cart <span class="badge"><%= session[:msale_cart].count unless session[:msale_cart].nil? || session[:msale_cart].empty? %></span>
        </a>
    </li>
    <li>
    	<a href="<%= debts_path %>"><span class="glyphicon glyphicon-piggy-bank" aria-hidden="true"></span> Debts</a>
    </li>
    <li>
    	<a href="#"><span class="glyphicon glyphicon-briefcase" aria-hidden="true"></span> Postpone sales <span class="badge">30</span></a>
    </li>
    <li>
    	<a href="<%= chasses_path %>"><span class="glyphicon glyphicon-gift" aria-hidden="true"></span> Warranted Machines</a>
    </li>
    <li>
    	<a href="#"><span class="glyphicon glyphicon-warning-sign" aria-hidden="true"></span> Out of stock <span class="badge">9</span></a>
    </li>
    <!-- only for sale staff -->
    <li>
        <a href="#"><span class="glyphicon glyphicon-usd" aria-hidden="true"></span> Sales Cart <span class="badge">12</span></a>
    </li>

  </ul>
</nav>

<%= session[:msale_cart] %>